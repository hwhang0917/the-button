services:
  thebutton:
    image: ghcr.io/hwhang0917/the-button:latest
    ports:
      - "8080:8080"
    environment:
      QUOTA: ${QUOTA:-10}
      EVENTS_DIR: /data/events
    volumes:
      - data:/data
    restart: unless-stopped

volumes:
  data:
